<h1>編集画面</h1>

<%= form_with model: @user, url: public_users_path(current_user), method: :patch do |f| %>

<h4>プロフィール画像</h4>
 <%= f.file_field :image, accept: "image/*" %>
<h4>氏名</h4>
  <%= f.text_field :name %>
<h4>メールアドレス</h4>
  <%= f.text_field :email %>
  <%= f.submit '更新する' %>
<% end %>
